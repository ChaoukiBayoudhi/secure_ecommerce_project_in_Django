# ============================================================================
# Django Secure E-Commerce Project - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# Command: cp env.example.txt .env
# NEVER commit .env to version control!
# ============================================================================

# ============================================================================
# DJANGO CORE SETTINGS
# ============================================================================

# Django Secret Key - Generate a new one for production!
# Generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-here-change-in-production

# Debug Mode - Set to False in production!
# WARNING: Never set DEBUG=True in production as it exposes sensitive information
DEBUG=True

# Allowed Hosts - Comma-separated list of host/domain names
# In production, list your actual domain names
# Example: ALLOWED_HOSTS=localhost,127.0.0.1,yourdomain.com,www.yourdomain.com
ALLOWED_HOSTS=localhost,127.0.0.1

# ============================================================================
# CORS (Cross-Origin Resource Sharing) CONFIGURATION
# ============================================================================

# CORS Allowed Origins - Comma-separated list of allowed origins
# In production, specify your frontend domain(s)
# Example: CORS_ALLOWED_ORIGINS=http://localhost:3000,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8000

# ============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# ============================================================================

# Database Name
DB_NAME=secure_ecommerce_db

# Database User
DB_USER=postgres

# Database Password
DB_PASSWORD=your-database-password

# Database Host
DB_HOST=localhost

# Database Port
DB_PORT=5432

# Database Connection Timeout (in seconds)
# Time to wait for a connection to be established
DB_CON_TIMEOUT=10

# Database Connection Max Age (in seconds)
# Time to keep the connection alive (0 = disable persistent connections)
# Recommended: 600 (10 minutes) for production
DB_CON_MAX_AGE=0

# Database SSL Mode
# Options:
#   - 'disable': No SSL encryption (development only)
#   - 'prefer': Prefer SSL encryption, but allow non-SSL connections
#   - 'require': Require SSL encryption, and reject non-SSL connections (recommended for production)
#   - 'verify-ca': Verify the certificate against a trusted CA
#   - 'verify-full': Verify the certificate against a trusted CA and the server's hostname (most secure)
DB_SSLMODE=prefer

# ============================================================================
# HTTPS/SSL SECURITY CONFIGURATION
# ============================================================================

# Force all HTTP requests to redirect to HTTPS
# Set to True in production when SSL is properly configured
# WARNING: Only enable after SSL certificate is set up, otherwise you'll get redirect loops!
SECURE_SSL_REDIRECT=False

# Ensure session cookies are only sent over HTTPS
# Set to True in production
# When True, session cookies will only be sent over HTTPS connections
SESSION_COOKIE_SECURE=False

# Ensure CSRF cookies are only sent over HTTPS
# Set to True in production
# When True, CSRF cookies will only be sent over HTTPS connections
CSRF_COOKIE_SECURE=False

# HTTP Strict Transport Security (HSTS) - Seconds
# Forces browsers to only use HTTPS for a specified period
# 0 = disabled (development)
# Recommended for production: 31536000 (1 year)
# WARNING: Once set and browsers cache it, you can't easily disable it!
SECURE_HSTS_SECONDS=0

# HSTS Include Subdomains
# If True, HSTS will apply to all subdomains
# Set to True in production if you have subdomains
SECURE_HSTS_INCLUDE_SUBDOMAINS=False

# HSTS Preload
# If True, allows your site to be included in browser HSTS preload lists
# Requires SECURE_HSTS_SECONDS > 0
# Only enable after thorough testing!
SECURE_HSTS_PRELOAD=False

# ============================================================================
# JWT TOKEN SETTINGS (Optional - can be configured in settings.py)
# ============================================================================

# Access Token Lifetime (in minutes)
# Default: 30 minutes (configured in settings.py)
# JWT_ACCESS_TOKEN_LIFETIME=30

# Refresh Token Lifetime (in days)
# Default: 1 day (configured in settings.py)
# JWT_REFRESH_TOKEN_LIFETIME=1

# ============================================================================
# STATIC FILES & MEDIA (Optional)
# ============================================================================

# Static Files URL
# STATIC_URL=/static/

# Media Files URL (if you have user uploads)
# MEDIA_URL=/media/

# Static Files Root Directory
# STATIC_ROOT=/path/to/staticfiles/

# Media Files Root Directory
# MEDIA_ROOT=/path/to/media/

# ============================================================================
# EMAIL CONFIGURATION (Optional - for production)
# ============================================================================

# Email Backend
# Options: 'django.core.mail.backends.smtp.EmailBackend' (SMTP)
#          'django.core.mail.backends.console.EmailBackend' (console - development)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Email Host (for SMTP)
# EMAIL_HOST=smtp.gmail.com

# Email Port
# EMAIL_PORT=587

# Email Host User
# EMAIL_HOST_USER=your-email@gmail.com

# Email Host Password
# EMAIL_HOST_PASSWORD=your-app-password

# Email Use TLS
# EMAIL_USE_TLS=True

# Default From Email
# DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ============================================================================
# LOGGING CONFIGURATION (Optional)
# ============================================================================

# Log Level
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
# LOG_LEVEL=INFO

# Log File Path
# LOG_FILE=/path/to/logs/django.log

# ============================================================================
# CACHE CONFIGURATION (Optional - for production performance)
# ============================================================================

# Cache Backend
# Options: 'django.core.cache.backends.redis.RedisCache'
#          'django.core.cache.backends.memcached.PyMemcacheCache'
#          'django.core.cache.backends.locmem.LocMemCache' (development)
# CACHE_BACKEND=django.core.cache.backends.locmem.LocMemCache

# Redis Cache Location (if using Redis)
# CACHE_LOCATION=redis://127.0.0.1:6379/1

# ============================================================================
# THIRD-PARTY API KEYS (Optional - add as needed)
# ============================================================================

# Payment Gateway API Key (e.g., Stripe, PayPal)
# STRIPE_SECRET_KEY=sk_test_your_stripe_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key

# AWS S3 Configuration (if using S3 for media files)
# AWS_ACCESS_KEY_ID=your-aws-access-key
# AWS_SECRET_ACCESS_KEY=your-aws-secret-key
# AWS_STORAGE_BUCKET_NAME=your-bucket-name
# AWS_S3_REGION_NAME=us-east-1

# ============================================================================
# PRODUCTION NOTES
# ============================================================================
# Before deploying to production:
# 1. Generate a new SECRET_KEY
# 2. Set DEBUG=False
# 3. Update ALLOWED_HOSTS with your domain
# 4. Update CORS_ALLOWED_ORIGINS with your frontend domain
# 5. Set all HTTPS settings to True/appropriate values
# 6. Set DB_SSLMODE=require or verify-full
# 7. Configure proper database credentials
# 8. Set up email backend
# 9. Configure logging
# 10. Set up proper static/media file serving

